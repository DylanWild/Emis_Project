import React from 'react'
import DialogueBox from './components/DialogueBox'

export default function Clickable(e) {
    
        if (e.type === 'click') {
          ('Left click', onclick().getElementById('.DialogueBox').style(display ('block')))
        } else {

            display: none

        }
    
    return (

        <div>
            <DialogueBox />
        </div>
    )
}
